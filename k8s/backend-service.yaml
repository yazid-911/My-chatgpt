apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: my-chatgpt-backend
spec:
  type: ClusterIP
  selector:
    app: my-chatgpt-backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
